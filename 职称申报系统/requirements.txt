PyQt5>=5.15.0
cryptography>=41.0.0
openai>=1.0.0
Pillow>=10.0.0
python-docx>=1.0.0
requests>=2.31.0
playwright>=1.40.0
# 安装后还需运行：playwright install chromium
# 可选依赖（按需安装）：
# paddlepaddle paddleocr   — OCR识别
# opencv-python numpy      — 图片纠偏
